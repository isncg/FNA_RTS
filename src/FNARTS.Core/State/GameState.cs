namespace FNARTS.Core
{
    /// <summary>
    /// Top-level game state machine states.
    /// Phase 1: Loading, MainMenu, Playing, Paused.
    /// </summary>
    public enum GameState
    {
        Loading,
        MainMenu,
        Playing,
        Paused
    }
}
